# Azure backend URL (server-only — not exposed to browser)
AZURE_API_URL=https://get-your-own.azurewebsites.net

# Azure auth key (server-only — not exposed to browser)
# Route handlers attach this as X-POLICY-SIM-KEY header when forwarding requests
AZURE_POLICY_SIM_KEY=your_key_here
